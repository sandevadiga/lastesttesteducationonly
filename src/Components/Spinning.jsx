import React from "react";
import styles from "./Spinning.module.css"; // Import CSS module

const Spinning = () => {
  return <div className={styles.spinner}></div>;
};

export default Spinning;
